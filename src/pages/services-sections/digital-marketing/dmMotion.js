/**
 * Re-export shim: bundlers/devtools often resolve extensionless `dmMotion` to `.js` first.
 * Implementation lives in `dmMotion.jsx` (JSX: DMDivider + motion primitives).
 */
export * from "./dmMotion.jsx";
