/** Smooth deceleration — default for section text and cards */
export const EASE_SMOOTH = [0.22, 1, 0.36, 1];

/** Service hero mount */
export const EASE_HERO_ENTRANCE = [0.16, 1, 0.3, 1];

/** Slight overshoot (process header, why-us cards) */
export const EASE_OVERSHOOT = [0.34, 1.56, 0.64, 1];

/** Stat counter & icon springs — reuse with framer animate() */
export const EASE_COUNTER = EASE_SMOOTH;
